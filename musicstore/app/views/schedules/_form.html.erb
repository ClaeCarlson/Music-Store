<%= form_with(model: schedule, local: true) do |form| %>
  <% if schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(schedule.errors.count, "error") %> prohibited this schedule from being saved:</h2>

      <ul>
      <% schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :schedule_slot %>
    <%= form.text_field :schedule_schedule_slot, id: :schedule_schedule_slots %>
    <%= form.label :shift %>
    <%= form.text_field :shift, id: :schedule_shift %>

  </div>


  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
